---
- name: Docker Container Deployment Playbook
  hosts: webservers
  become: true
  vars:
    NEXUS_REPO: "nexus.odochidevops.space"

  tasks:

    - name: Wait for Docker to be ready
      shell: until docker info; do sleep 5; done
      
    - name: Stop running container
      shell: docker stop appContainer || true

    - name: Remove stopped container
      shell: docker rm appContainer || true

    - name: Remove existing image
      shell: docker rmi "{{ NEXUS_REPO }}/nexus-docker-repo/apppetclinic:latest" || true

    - name: Log into private Nexus registry
      become_user: ubuntu
      shell: echo "admin123" | docker login "{{ NEXUS_REPO }}" -u admin --password-stdin

    - name: Pull latest image from Nexus
      become_user: ubuntu
      command: docker pull "{{ NEXUS_REPO }}/nexus-docker-repo/apppetclinic:latest"

    - name: Run container
      become_user: ubuntu
      shell: |
        docker run -d \
        --name appContainer \
        -p 8080:8080 \
        --restart always \
        "{{ NEXUS_REPO }}/nexus-docker-repo/apppetclinic:latest"